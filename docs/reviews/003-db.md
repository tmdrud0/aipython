# 리뷰: 003-db

- 대상 스펙: docs/specs/003-db.md
- 판정: **PASS**
- 변경 규모: 3 files, +637 / -0 (전부 신규. 수정 파일 0개)
  - `docs/specs/003-db.md` (326줄, 명세)
  - `t2s/db.py` (114줄)
  - `tests/test_db.py` (197줄)

## 실행한 검증

`git status --porcelain`, `git diff --stat` 로 확인: untracked 3개가 전부이고 **tracked 파일은 하나도 수정되지 않았다.**

| 명령 | 결과 |
|---|---|
| `git diff --stat t2s/guard.py t2s/config.py` | **빈 출력** — guard·config 무변경 (스펙 요구 충족) |
| `docker compose -f docker/docker-compose.yml up -d` | 컨테이너가 재생성되어 재기동됨. health check 가 healthy 가 될 때까지 대기 후 테스트 실행 |
| `.\.venv\Scripts\python.exe -m pytest tests/test_db.py -v` | **23 passed, 0 failed, 0 error, 0 skipped** (2.48s) |
| `.\.venv\Scripts\python.exe -m pytest tests/ -q` | **73 passed, 0 failed, 0 error, 0 skipped** (2.52s). 001(12) + 002(38) + 신규(23) = 73. 스펙 기대 "50 passed 보다 큰 수" 충족 |
| `python -c "from t2s.guard import ensure_safe_sql as f; f('SELECT * FROM information_schema.TABLES')"` | `t2s.guard.UnsafeSQLError: system schema access is not allowed: information_schema` — guard 가 여전히 system schema 를 막음 |
| db.py import AST 검사 | `['dataclasses', 'pymysql', 'pymysql.err', 't2s.config', 't2s.guard']` — 스펙 기대 5개와 정확히 일치. `os`·`dotenv` 없음 |

전부 실제 실행한 출력이며, 예측이 아니다.

## 수용 기준 대조

수용 기준 23행 전부가 `tests/test_db.py` 에 1:1 로 존재하고(파일에 `# 1`~`# 23` 주석으로 번호가 붙어 있어 대조 가능), 실제 실행에서 전부 통과했다.

| # | 기준 | 결과 | 근거 |
|---|---|---|---|
| 1 | `run_query("SELECT COUNT(*) FROM film")` 3필드 완전 단언 | 통과 | tests/test_db.py:19-23 |
| 2 | 별칭 `n` 이 컬럼명 | 통과 | tests/test_db.py:27-29 |
| 3 | 기존 LIMIT 3 보존, 행 3개 완전 일치 | 통과 | tests/test_db.py:33-40 |
| 4 | LIMIT 1000 인데 200행으로 제한 (이중 방어) | 통과 | tests/test_db.py:44-46 |
| 5 | `max_rows=7` 인자 전달 | 통과 | tests/test_db.py:50-52 |
| 6 | 두 QueryResult `==` 비교 (frozen) | 통과 | tests/test_db.py:56-59 |
| 7 | DROP → `UnsafeSQLError`, 메시지 완전 일치 + `type()` 단언 | 통과 | tests/test_db.py:63-67 |
| 8 | LLM 경로에서 information_schema 차단 | 통과 | tests/test_db.py:71-74 |
| 9 | 다중 문장 거부 | 통과 | tests/test_db.py:78-81 |
| 10 | 1146 → `QueryError` 접두사 비교 | 통과 | tests/test_db.py:85-88 |
| 11 | 1064 문법 오류 → `QueryError` | 통과 | tests/test_db.py:92-95 |
| 12 | `db_port=1` 접속 실패 → `QueryError` (Settings 직접 생성) | 통과 | tests/test_db.py:99-111 |
| 13 | 컬럼 총수 131 | 통과 | tests/test_db.py:115-116 |
| 14 | 테이블 23개 | 통과 | tests/test_db.py:120-121 |
| 15 | BASE TABLE 16 / VIEW 7 (두 단언 모두) | 통과 | tests/test_db.py:125-128 |
| 16 | 첫 행 `actor.actor_id` 완전 일치 | 통과 | tests/test_db.py:132-141 |
| 17 | `film.rating` enum / True / 빈 key | 통과 | tests/test_db.py:145-154 |
| 18 | `film.title` varchar / False / MUL | 통과 | tests/test_db.py:158-167 |
| 19 | film 컬럼 13개 | 통과 | tests/test_db.py:171-173 |
| 20 | tuple + 전부 ColumnInfo | 통과 | tests/test_db.py:177-180 |
| 21 | `connect` 후 `open` True → close 후 False | 통과 | tests/test_db.py:184-188 |
| 22 | `fetch_schema` 시그니처 `["settings"]` | 통과 | tests/test_db.py:192-193 |
| 23 | `run_query` 시그니처 3개, 우회 인자 없음 | 통과 | tests/test_db.py:197-198 |

빠진 케이스 없음. 기대값을 코드에 맞춰 바꾼 테스트 없음 (기대값이 스펙 표와 글자 그대로 일치함을 확인).

## 발견 사항

### 심각

없음.

### 경미

1. **계약 일치 — 전 항목 확인 (문제 아님, 근거 기록)**
   - `QueryError`/`QueryResult`/`ColumnInfo`/`SCHEMA_SQL`/`connect`/`run_query`/`fetch_schema` 시그니처·필드 순서가 스펙과 글자 그대로 일치 (t2s/db.py:10-39, 42, 60, 87).
   - `SCHEMA_SQL` 이 스펙 문자열과 글자 그대로 동일하고 `%s` 바인딩 사용 (t2s/db.py:31-39, `cursor.execute(SCHEMA_SQL, (settings.db_name,))` t2s/db.py:92).
   - `run_query` 는 guard 를 **try 밖에서** 호출해 예외를 감싸지 않고 그대로 올림 (t2s/db.py:61). `connect` 이후에 `try/finally close()` (t2s/db.py:63-82). `fetchmany(max_rows)` 사용, `fetchall` 아님 (t2s/db.py:80). `sql=safe_sql` (정규화된 문자열) 반환 (t2s/db.py:84).
   - `fetch_schema` 는 `sql` 파라미터가 없고 guard 를 호출하지 않음, `fetchall` 사용 (t2s/db.py:87-101). `is_nullable == "YES"` 변환 (t2s/db.py:111).
   - pymysql 예외 정확히 4종만 잡고 `except Exception` 없음 (t2s/db.py:68-73, 93-98). 커넥션 풀/캐시/commit/autocommit/로깅 없음.
   - `db.py` 는 `os.environ`·`load_settings` 를 건드리지 않고 `Settings` 를 인자로만 받음 (설계결정 1 준수).

2. **[경미] 테스트가 `db_password="t2s_ro_pw"` 를 소스에 박음** — tests/test_db.py:103. 스펙 12행이 "Settings 를 직접 만들어 넘긴다"고 요구했기 때문에 필요한 결과다. 이 값은 이미 `docker/initdb/03-readonly-user.sql:3` 과 `.env.example` 에 커밋된 로컬 개발 컨테이너용 자격증명이라 **새로운 노출은 아니다.** 다만 004 이후 진짜 비밀이 필요한 테스트가 생기면 이 패턴(가짜 값 하드코딩)을 그대로 따르지 않도록 주의. 조치 불요.

3. **[경미] 예상 분량 초과** — 스펙 예상 "약 200줄" 대비 실제 311줄(db+test). 주석·빈줄 포함 자연스러운 차이라 범위 이탈 아님. 조치 불요.

## 커밋 전 조치

없음.

## 구현 세션 재작업 지시문

해당 없음 (PASS).