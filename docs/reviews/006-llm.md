# 리뷰: 006-llm

- 대상 스펙: docs/specs/006-llm.md
- 판정: PASS
- 변경 규모: 2 files (신규 2 / 수정 0), 약 +240 줄 (`t2s/llm.py` 51줄, `tests/test_llm.py` 189줄)

## 실행한 검증

검증자가 스펙 "검증 명령" 절을 직접 실행했다. 구현 세션의 보고에 의존하지 않았다.

| 명령 | 결과 |
|---|---|
| `python -m pytest tests/test_llm.py -v` | **20 passed, 0 failed, 0 error, skipped 0** (1.89s) |
| `python -m pytest tests/test_llm.py -v -k live` | `test_generate_sql_live_smoke PASSED` — **SKIPPED 아님** (1.49s) |
| `python -m pytest tests/ -q` | **164 passed** (144 기존 + 20 신규, 깨진 것 없음) |
| 무의존성 확인 (AST) | `['ollama', 't2s.config', 't2s.prompt']` — 스펙 기대와 글자 그대로 일치. `t2s.db`/`t2s.guard`/`json`/`re`/`time` 없음 |
| `git diff --stat t2s/guard.py t2s/prompt.py t2s/config.py t2s/db.py t2s/schema.py` | **빈 출력** — 기존 모듈 무변경 |
| `git status --porcelain` | 신규 2개(`t2s/llm.py`, `tests/test_llm.py`, `docs/specs/006-llm.md`)만 존재. 스펙 범위 외 수정 없음 |
| end-to-end 수동 확인 | 아래 참조 |

end-to-end 실제 출력 (스펙의 수동 확인 명령을 그대로 실행):

```
Extraction(sql='SELECT COUNT(*) AS total_films
FROM film', kind='sql')
QueryResult(columns=('total_films',), rows=((1000,),), sql='SELECT COUNT(*) AS total_films FROM film LIMIT 200')
```

행에 `1000` 이 보인다 — 스펙 기대치 충족. 프로젝트 최초 end-to-end 연결 성공.

## 수용 기준 대조

26행 전부가 테스트로 존재하고 전부 통과했다.

| # | 기준 | 결과 | 근거 |
|---|---|---|---|
| 1~7 | 정상 경로 + 호출 인자 (calls 길이, 키 집합, model, options, messages, SYSTEM_PROMPT) | 통과 | tests/test_llm.py:39-55 |
| 8~12 | 응답 형태별 (json 펜스, ``` 펜스, UNSUPPORTED, unparsable, 빈 본문) | 통과 | tests/test_llm.py:59-83 |
| 13~15 | `content` 키 없음 / `message` 키 없음 / non-str 본문 → `unparsable`, 예외 없음 | 통과 | tests/test_llm.py:87-101 |
| 16 | ConnectionError → 완전 일치 메시지, 원본 문구 `boom` 배제 | 통과 | tests/test_llm.py:105-110 |
| 17 | ResponseError → `startswith("ollama error: ")` | 통과 | tests/test_llm.py:114-118 |
| 18 | RequestError → 접두사 비교 | 통과 | tests/test_llm.py:122-126 |
| 19 | `__cause__` 가 원본 ConnectionError | 통과 | tests/test_llm.py:130-134 |
| 20 | ValueError 통과, `except Exception:` 없음 | 통과 | tests/test_llm.py:138-143 |
| 21 | `generate_sql` 파라미터에 `timeout` 없음 | 통과 | tests/test_llm.py:147-153 |
| 22 | `build_client` 파라미터 | 통과 | tests/test_llm.py:157-158 |
| 23 | `build_client` 네트워크 없이 Client 반환 | 통과 | tests/test_llm.py:162-164 |
| 24 | 상수 60.0 / 0.0 | 통과 | tests/test_llm.py:168-170 |
| 25 | `LLMError` is `RuntimeError` | 통과 | tests/test_llm.py:174-175 |
| 26 | 라이브 스모크, 정답 단언 없음, skip 경로 2종 | **PASSED (실측)** | tests/test_llm.py:179-189 |

## 계약 준수

- `t2s/llm.py` 시그니처가 스펙과 글자 그대로 일치: `LLMError(RuntimeError)`, `DEFAULT_TIMEOUT=60.0`, `TEMPERATURE=0.0`, `build_client(settings, timeout=DEFAULT_TIMEOUT)`, `generate_sql(schema_text, question, settings, client=None)` (t2s/llm.py:7-24)
- import 가 스펙 지정 4줄 그대로 (t2s/llm.py:1-4)
- `build_client` 가 `ollama.Client(host=..., timeout=timeout)` 만 반환, 예외 처리·로깅 없음 (t2s/llm.py:15-16)
- `client.chat` 키워드 인자로 정확히 3개(`model`/`messages`/`options={"temperature": TEMPERATURE}`), `think`/`format`/`stream`/`keep_alive` 없음 (t2s/llm.py:31-35). FakeClient 가 `**kwargs` 만 받아 위치 인자 사용 시 TypeError 로 드러나는 강제 장치가 테스트에 있고 통과했다
- 예외 3종만 잡고 `ConnectionError` 를 먼저 잡음. `raise ... from exc` 사용, ConnectionError 원본 문구 미포함 (t2s/llm.py:36-41). `except Exception:` 없음 — #20 테스트가 통과로 증명
- 응답 방어 접근: `KeyError`/`TypeError` → 빈 문자열, non-str → 빈 문자열, `LLMError` 던지지 않음 (t2s/llm.py:43-49). 스펙의 1·2단계 분리 try 를 하나의 try 로 합쳤으나 동작은 완전히 동일하다 (예외 종류와 처리 경로 동일)
- `extract_sql(content)` 결과를 가공 없이 반환 (t2s/llm.py:51)
- `ensure_safe_sql` 미호출, `t2s.db` 미import — AST 검증으로 확인 (설계결정 5·6 준수)
- `unittest.mock` 미사용, 스펙 지정 `FakeClient` 형태 그대로 (tests/test_llm.py:17-27)
- `load_settings()` 라이브 스모크 1곳에서만 호출 (tests/test_llm.py:181)

## 보안 경계

- 자격증명 없음: `llm.py` 에 호스트·비밀번호 하드코딩 없음. 테스트의 `FAKE_SETTINGS` 는 스펙이 지정한 더미 값(`u`/`p`) 그대로 (tests/test_llm.py:30-33)
- `llm.py` 는 SQL 을 실행·검사하지 않는다. guard 우회 경로 없음 (설계결정 5·6 준수)
- `.env` 를 코드에 넣지 않았고, `docs/`/`requirements.txt` 무변경

## 발견 사항

심각 항목 없음.

### [경미] 스펙 "질문" 절과의 실측 확인 — 문제 없음을 명시

- 실측 1: `ollama.ResponseError("...")` 의 `str()` 뒤에 `(status code: -1)` 이 붙는다는 스펙의 기술은 실제로 확인했다. 테스트 #17 이 접두사 비교이므로 영향 없음.
- 실측 2: 라이브 스모크는 로컬 ollama(`http://127.0.0.1:11434`, 모델 `glm-5.3-flash:cloud`)를 경유해 1.5s 내에 응답했다. 느린 클라우드 호출임을 고려한 스펙의 우려와 달리 실측상 빠르지만, 재시도·타임아웃 로직이 없다는 설계결정 2 는 여전히 유효하다.

## 스펙 오류 여부 / 구현 세션 절차 준수

스펙 오류로 인한 문제는 없었다. 스펙의 배경 절(죽은 호스트가 내장 `ConnectionError`), 계약 3·4·5·7, 설계결정 1~8 은 모두 구현과 일치했고, 실측 기반 서술이 구현을 정확히 안내했다. 구현 세션의 절차 준수를 판단할 근거(스펙 오류)도 없었다.

## 커밋 전 조치

없음. 그대로 커밋 가능.